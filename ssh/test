#!/bin/bash
set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/_common.sh"

init_infrastructure

echo "🧪 Iniciando suite de tests en entorno Docker..."

docker compose build test
docker compose run --rm test "$@"
