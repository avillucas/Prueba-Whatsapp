#!/bin/bash
set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/_common.sh"

init_infrastructure

echo "🔍 Iniciando linter (ESLint & TypeScript check) en entorno Docker..."

docker compose build test
docker compose run --rm test npm run lint "$@"
